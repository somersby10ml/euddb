export interface Address {
  id: string;
  address: string;
  version: string;
  platform: string;
  pid: string;
  byte: string;
  name: string;
  size: string;
  length: string;
  scr: string;
  description: string;
}
